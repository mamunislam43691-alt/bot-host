# nixpacks.toml — Railway build configuration

[phases.setup]
nixPkgs = [
  'python312',
  'python312Packages.pip',
  'nodejs_22',
  'gcc',
  'stdenv.cc.cc.lib',
  'glibc',
  'gnumake',
  'pkg-config',
  'openssl',
  'libffi',
  'zlib',
  'libjpeg',
  'libpng',
  'freetype',
  'libxml2',
  'libxslt',
  'ffmpeg',
  'bash',
  'curl',
  'wget',
  'git',
]

[phases.install]
cmds = [
  'npm install',
  # Build time-এই common compiled packages pre-install করো
  # এতে runtime-এ আর compile করতে হবে না, pre-built wheel use হবে
  'python3 -m pip install --prefer-binary --quiet grpcio firebase-admin google-cloud-firestore numpy pandas Pillow opencv-python-headless cryptography lxml aiohttp pydantic',
]

[start]
cmd = 'node server.js'
